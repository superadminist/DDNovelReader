# DDNovelReader 原型等效重构与分阶段交付计划

## 1. 总体目标

当前效果不符原型的根因是：使用 Tkinter 控件重新临摹 React/CSS 原型，并保留隐藏旧控件和旧 UI Mixins 作为兼容层。由此产生双标题栏、方角卡片、统一 DD 封面、Unicode 图标、排版比例错误和悬浮窗结构偏差。

最终方案确定为：

- 使用 PySide6 WebEngine 直接承载现有 React/CSS 原型。
- `prototype/src` 是唯一 UI 视觉与结构来源，不再使用 Tkinter 控件临摹。
- 彻底删除旧 Tkinter 界面、隐藏兼容控件和旧 UI 调用链。
- 保留书籍解析、书架存储、导入、章节、缓存、进度、书签和搜索等非 UI 能力。
- 全程序只创建一个现有 `SpeechController`。
- 主窗口和独立悬浮朗读窗共享同一 TTS、章节、字符偏移和阅读进度。
- 原型是唯一 UI 标准，不接受以“平台限制”为理由的主动降级。

## 2. 计划模式与交付规则

- 整项任务按“计划模式 → 实施 → 验证 → 阶段交付”的循环推进。
- 每个阶段开始前必须处于计划模式，只允许检查代码、差异、调用链和验证条件，不修改文件。
- 每阶段实施前先列明本阶段目标、涉及文件、接口变化、风险和验收标准；只有用户明确确认并切换到执行模式后才实施该阶段。
- 每阶段只完成当前范围，不得提前夹带下一阶段功能。
- 阶段完成后必须交付：
  - 实际修改清单；
  - Git 差异摘要；
  - 自动化测试结果；
  - 原型与成品截图对照；
  - 手工验证结果；
  - 未验证项和已知问题。
- 当前阶段验收失败时停止进入下一阶段，先在计划模式分析失败原因和修复方案。
- 每个阶段交付后停止，等待用户验收，不得自动进入下一阶段。
- 只有全部阶段通过才能标记任务完成；测试通过但视觉不一致，或截图相似但真实 TTS 未验证，都不能算完成。

## 3. 技术架构与接口

- 桌面容器：PySide6 + `QWebEngineView`。
- 前后端通信：`QWebChannel`。
- 主窗口：无边框 Qt 窗口，只显示原型内部标题栏，不能出现双标题栏。
- 悬浮窗：独立、无父窗口、无边框、可置顶的 Qt 顶层窗口。
- 窗口拖动和缩放：优先使用 `QWindow.startSystemMove()` 和 `startSystemResize()`。
- 前端：直接把 `prototype/src` 改造成正式生产前端，只替换模拟数据与模拟事件，不复制或重新设计另一套 UI。
- 后端：把现有非 UI 业务从各 `*_ui.py` 中提取为不依赖 Tk 的服务。
- TTS：由唯一 Qt 定时器调用 `SpeechController.drain()`，统一向主窗口和悬浮窗广播状态。
- Bridge 使用固定 `schemaVersion` 和对应 TypeScript 类型；异常统一转换成用户可理解的业务提示。
- 不改变现有 `library.json` 字段含义；新增悬浮窗设置继续使用现有默认值合并机制。
- 打包时将 Vite 产物、WebEngine 资源、图标和封面全部收入 PyInstaller 包；运行时不得依赖 Node、Vite 开发服务器或本机 Python。

## 4. 分阶段实施与验收门禁

### 阶段 0：基线与拆分计划

本阶段只允许只读检查和非持久化验证，不允许修改、删除、重命名或格式化项目文件。

- 记录当前脏工作区，区分用户原有修改、上次错误 UI 实现、Tcl/Tk 启动修复和原型资料。
- 禁止使用 `git reset`、`git checkout`、`git clean` 或其他覆盖方式。
- 不得触碰 `sample/星火.docx`、`sample/星火.epub` 及其他无关用户文件。
- 绘制书籍导入、打开、章节切换、进度保存、TTS 事件、高亮和退出清理调用链。
- 找出所有旧 UI 耦合，包括 Tk 控件引用、隐藏兼容控件和依赖控件属性的业务代码。
- 列出可以原样保留的非 UI 模块，以及必须从 `*_ui.py` 提取的业务逻辑。
- 检查 PySide6、WebEngine、Node、npm/pnpm 和 PyInstaller 是否可用；只检查，不安装依赖。
- 使用临时 `APPDATA` 运行安全测试，避免修改真实 `library.json`；不运行会覆盖样本或项目内容的测试。
- 固化内容库、阅读器目录展开/收起、粘贴弹窗、深色模式、悬浮窗默认/最小/放大状态的当前原型截图。

验收门禁：差异归属明确、非 UI 能力边界明确、原型可访问、现有数据可读取、基线测试结果完整。

阶段交付必须包含：总体结论、差异归属表、调用链、旧 UI 耦合清单、非 UI 能力清单、测试基线、原型截图、环境检查、阶段 1 决策完整计划、未验证项和阻塞项。

### 阶段 1：PySide6 宿主与生产前端

- 新建 PySide6 主窗口、WebEngine 页面和 QWebChannel 基础桥接。
- 将 `prototype/src` 作为正式 UI 源码，不复制第二套样式。
- 接入真实内容库只读数据和窗口控制。
- 暂时保留旧入口用于回归对照，但新宿主不得导入任何旧 UI 组件。

验收门禁：新入口可离线启动、只有一层标题栏、内容库真实数据显示正确，1280×720 截图无结构差异。

### 阶段 2：内容库与导入

- 接入真实书架、进度、搜索、文件导入和粘贴文本。
- 使用原型规定的真实封面资产与缺省封面规则。
- 网页、播客和音频入口保持明确不可用状态，不实现伪功能。
- 导入成功后刷新内容库并进入阅读器。

验收门禁：有书、空书架、搜索结果、搜索空状态、粘贴弹窗和导入失败状态全部通过测试及截图对照。

### 阶段 3：阅读器与唯一 TTS

- 接入章节目录、正文、阅读进度、字号、排版、搜索和书签。
- 接入播放、暂停、停止、上一句、下一句、语速、音量及语音选择。
- 朗读事件驱动 React 当前句高亮，不恢复全文高频扫描。
- 主阅读器只订阅后端状态，不创建自己的播放状态。

验收门禁：确认仅一个 `SpeechController` 和一个事件消费入口；章节、偏移、高亮、进度与播放状态一致；2 万字和 10 万字内容操作无明显卡顿。

### 阶段 4：独立悬浮朗读窗

- 按原型实现上一句、当前句、下一句、控制条、置顶、关闭、字号、背景、透明度和缩放手柄。
- 使用系统级拖动及右下角缩放，释放后再保存几何信息。
- 主窗口最小化时悬浮窗继续显示和控制朗读。
- 关闭悬浮窗只关闭显示，不停止 TTS。

验收门禁：默认、最小、放大和长句状态截图一致；单/双显示器、窗口边界、重启恢复、置顶和主窗口最小化行为通过验证。

### 阶段 5：彻底删除旧 UI 并切换入口

- 将仍混在 `*_ui.py` 中的非 UI 业务代码提取为服务。
- 删除 `modern_ui.py`、`_build_legacy_ui`、`_build_compatibility`、隐藏 Treeview/Combobox 和 Tk UI Mixins。
- 删除应用运行路径中的 `tkinter`、`ttk`、`tkinterdnd2` 和 Tcl/Tk 启动逻辑。
- 将正式入口切换到 PySide6；删除只验证旧 Tk 控件的测试，改为服务、Bridge 和 React 测试。
- 不使用 Git 重置覆盖用户内容。

验收门禁：

- `novelreader` 正式运行代码中不存在 `import tkinter`、`tk.*`、`ttk.*` 或 `tkinterdnd2`。
- 不存在 `NovelReaderApp`、`ModernMethods`、`_build_legacy_ui` 或 `_build_compatibility`。
- 新程序完整覆盖原型 P0 路径，旧 UI 无入口、无隐藏实例、无残留依赖。

### 阶段 6：全量验证与打包交付

- 执行 Python 服务、Bridge、React 组件及桌面集成测试。
- 在相同窗口尺寸、数据和状态下完成逐屏视觉对照；外部系统阴影除外，结构差异必须为零，像素差异控制在约定阈值内。
- 验证 100%、125%、150%、200% DPI 和双显示器。
- 实测本地 SAPI5、Edge TTS、断网回退、扬声器播放和长文本持续朗读。
- 使用 PyInstaller 生成 EXE，确认不需要 Node、开发服务器或本机 Python 即可运行，且 WebEngine、图标和封面资源完整。

验收门禁：全量测试、关键手工路径、视觉截图、真实朗读、性能和打包均有明确结果；任何未验证项必须保持“未完成”，不能用静态检查代替。

## 5. 最终验收标准

- 页面结构、尺寸、间距、配色、圆角、阴影、图标、封面、文字层级及交互状态以原型为准。
- 旧 UI 代码和兼容控件已物理移除，而非仅停止调用。
- 主窗口与悬浮窗共享唯一 TTS、章节、字符偏移和阅读进度。
- 原有书架数据、书籍解析和缓存无需用户迁移。
- 每阶段均有独立交付记录与验证证据，全部门禁通过后才能完成任务。

## 6. 新会话使用方式

1. 创建新会话并切换到计划模式。
2. 将工作目录设置为 `D:\phpstudy_pro\WWW\DDNovelReader`。
3. 在新会话中发送以下提示词：

```text
请在 D:\phpstudy_pro\WWW\DDNovelReader 中继续 DDNovelReader 原型等效重构任务。

首先完整阅读根目录 REFACTOR_PLAN.md，并将它作为本任务的阶段范围、技术方案、执行门禁和验收标准。然后按其中“阶段 0：基线与拆分计划”执行。

本轮必须保持计划模式，只允许只读检查和非持久化验证，不得修改、删除、重命名或格式化任何项目文件。禁止使用 git reset、git checkout、git clean；不得触碰 sample/星火.docx、sample/星火.epub 和其他无关用户修改。

同时阅读 pd.md、HANDOFF.md、prototype/AGENTS.md、prototype/src/App.jsx、prototype/src/styles.css、prototype/design-qa.md、README.md、requirements.txt，以及当前 GUI、导入、阅读、存储、缓存、快捷键、TTS 和测试代码。

相关历史会话：
- codex://threads/01a09146-3108-7f31-986f-eea3bc34cdcd
- codex://threads/01a090fc-353e-7312-9beb-1244193bc1d3

原型地址：http://127.0.0.1:5173/

完成阶段 0 后，严格按 REFACTOR_PLAN.md 的阶段交付要求输出：差异归属、当前调用链、旧 UI 耦合、可保留的非 UI 能力、测试基线、原型截图、环境检查、阶段 1 的决策完整实施计划、未验证项和阻塞项。

阶段 0 完成后立即停止，不修改代码，不自动进入阶段 1，等待我验收并明确切换到执行模式。
```

4. 阶段 0 验收通过后，新会话中使用：

```text
阶段 0 验收通过。请先在计划模式复核 REFACTOR_PLAN.md 中阶段 1 的范围、修改文件、Bridge 接口、风险和验收命令；不要提前实施阶段 2。待我明确切换到执行模式后，再只实施阶段 1。阶段 1 完成并验证后停止，按文档要求交付结果。
```

5. 后续阶段沿用相同方式：每次先在计划模式复核一个阶段，再切换到执行模式只实施该阶段，验证交付后停止。
