# 多多朗读原型落地开发交接文档

> 交接目标：由 Codex 在 `D:\phpstudy_pro\WWW\DDNovelReader` 中把已确认的 Web 原型严格落地为 Windows 桌面应用。
>
> 产品需求：`D:\phpstudy_pro\WWW\DDNovelReader\pd.md`
>
> 唯一视觉原型：`D:\phpstudy_pro\WWW\DDNovelReader\prototype`
>
> 原始视觉素材：`E:\ForCodex\dailyRepair\video-ui-extraction\WeChat\_20260903141223\key-ui`

## 1. 一句话任务

完整实现原型中的 macOS 风格内容库、阅读器、导入弹窗、底部播放条和可拖动/可缩放的独立悬浮朗读窗；界面必须严格按原型复现，朗读功能必须接入项目现有实现，不得重写或复制第二套 TTS 状态。

## 2. 本次开发的硬性边界

### 2.1 必须严格按原型实现

- `prototype/src/App.jsx` 是页面结构、可见文案、控件层级与交互状态的实现基准。
- `prototype/src/styles.css` 是尺寸、间距、颜色、字号、圆角、阴影、透明度、布局与响应行为的实现基准。
- `prototype/design-qa.md` 是已经完成的视觉核对记录。
- `prototype/public/covers/` 是当前原型卡片封面资源；如最终应用需要打包，必须作为真实资源纳入打包配置。
- 不允许以现有 Tkinter 界面、系统默认控件样式或开发者个人偏好替换原型设计。
- 不允许只做一个“类似”的界面；每个关键状态都要与原型逐项对照。

### 2.2 只复用现有朗读能力

现有朗读链路必须保留并复用：

- `SpeechController` 实例和事件队列；
- SAPI5、Edge TTS、本地回退和 MCI 播放；
- 播放、暂停、停止、上一句、下一句；
- 语速、音量、语音选择；
- `chapter_idx`、`char_offset`、当前句定位和逐句高亮；
- 长文本朗读性能优化。

新界面只是现有朗读能力的视图和控制入口。不得新建第二个 `SpeechController`、第二条事件消费循环、第二份阅读进度或第二路音频。

### 2.3 可以复用但不能决定视觉的能力

为保持数据兼容，文件解析、书架、章节、缓存、进度、书签、搜索和设置存储应优先复用现有代码。这些代码只能提供数据与行为，不能反向限制新界面的布局和样式。

### 2.4 不得擅自扩展

- 不接入账号、云同步、在线书城或远程数据库。
- 不实现自动翻译；双语按钮只显示内容中已经存在且能可靠映射的译文。
- 不用 TTS 冒充播客导入、音频识别或语音转写。
- 网页提取、播客/音频属于后续能力；P0 中保留原型入口和明确的不可用说明即可。
- 不修改现有 `library.json` 字段含义，不要求用户手工迁移数据。

## 3. 当前基线

- 当前基线提交：`601c778`，说明为“优化长文本朗读性能”。
- 技术栈：Python、Tkinter/ttk、pyttsx3、Edge TTS、Windows MCI。
- 主应用：`novelreader/gui.py` 中的 `NovelReaderApp`，由多个 Mixin 组合。
- 设置存储：`%APPDATA%\DDNovelReader\library.json`，默认值在 `novelreader/storage.py` 的 `DEFAULT_SETTINGS`。
- 当前工作区不是干净状态。开发前必须先执行只读差异检查，区分用户已有修改、换行符/文件时间噪声和本次改动；禁止重置、覆盖或删除用户内容。
- `sample/星火.docx` 与 `sample/星火.epub` 是已有修改，和本需求无关，不得触碰。
- 根目录 `.pnpm-store/` 是原型依赖缓存残留，不属于桌面应用交付内容；不要为了本需求使用破坏性方式清理。

## 4. 必读顺序

Codex 开始编码前按以下顺序阅读：

1. `pd.md`：完整需求、范围、边界和验收标准。
2. `prototype/src/App.jsx`：所有页面、状态和交互。
3. `prototype/src/styles.css`：视觉参数和响应式规则。
4. `prototype/design-qa.md`：原型核对结果。
5. `README.md`：现有功能与用户使用方式。
6. `novelreader/gui.py`、`novelreader/tts_ui.py`、`novelreader/reader_ui.py`、`novelreader/storage.py`、`novelreader/import_ui.py`、`novelreader/shortcuts_ui.py`。
7. `tests/test_features.py` 及其他相关测试。

阅读完成后先输出：现有调用链、拟修改文件、状态共享方案、视觉复现方案和验证计划，再开始改动。

## 5. 原型页面与桌面实现映射

| 原型区域 | 必须实现的内容 | 建议代码落点 | 复用能力 |
| --- | --- | --- | --- |
| 应用外壳 | macOS 风格标题栏、左侧导航、主内容区、深色状态 | `novelreader/gui.py` 与独立样式/组件模块 | 主窗口生命周期、设置 |
| 内容库 | 搜索、四个导入入口、最近阅读卡片、空状态 | 建议新增 `novelreader/library_home_ui.py` | 书架数据、进度、封面、打开书籍 |
| 粘贴文本 | 标题、正文、取消、创建并阅读、校验错误 | `novelreader/import_ui.py` 或单独对话框模块 | 文本分章、缓存、书架写入 |
| 文件导入 | 原型入口及导入反馈 | `novelreader/import_ui.py` | 现有 `_add_book`、解析与备份 |
| 阅读器顶部 | 返回、目录、书名、字号、排版、悬浮朗读、设置 | 阅读器新视图模块，最小接线到 `gui.py` | `_toggle_toc`、`_change_font_size`、设置 |
| 章节目录 | 章节列表、当前高亮、章节数、点击切换 | `novelreader/reader_ui.py` | `_goto_chapter`、现有章节数据 |
| 正文 | 章节号、标题、正文、当前句高亮、进度 | `novelreader/reader_ui.py` | 现有文本定位与 `_highlight_sentence` |
| 底部播放条 | 章节、状态、前后句、播放/暂停、停止、进度、语速、音量、悬浮入口 | `novelreader/tts_ui.py` 与共享 UI 同步方法 | `_tts_toggle`、`_tts_stop`、唯一 TTS 状态 |
| 悬浮朗读窗 | 上一句、当前句、下一句、控制区、置顶、关闭、拖动、缩放、样式设置 | 建议新增 `novelreader/floating_ui.py` | 唯一 TTS、当前章节、字符偏移、设置 |
| 快捷键 | 打开/关闭悬浮窗及悬浮窗内控制 | `novelreader/shortcuts_ui.py` | 现有快捷键绑定方式 |
| 持久化 | 悬浮窗几何、置顶、透明度、字号、背景、双语 | `novelreader/storage.py` | 默认值合并与保存机制 |

文件名是建议，不是要求。若现有结构有更合适且更小的落点，可以调整；但必须保持职责清晰，不能把整套新界面堆进一个超长方法。

## 6. 状态与事件设计约束

### 6.1 单一事实来源

以下状态仍由 `NovelReaderApp` 和现有业务对象持有：

- 当前书籍：`current_bid`、`book`；
- 当前章节：`chapter_idx`；
- 当前朗读位置：`char_offset`；
- 播放状态：现有 `SpeechController`；
- 用户设置：现有 `settings`。

内容库、阅读器、底部播放条和悬浮窗均只观察和操作这些状态，不创建自己的业务副本。

### 6.2 TTS 事件只能消费一次

`novelreader/reader_ui.py::_handle_tts_event` 继续作为现有事件入口。建议在完成原有逻辑后调用一个统一视图同步方法，例如：

- `sentence_start`：同步主阅读区高亮、底部播放条、悬浮窗当前句和相邻句；
- `sentence_done`：同步偏移和进度，遵守现有保存节流；
- `finished`：同步完成状态并保存；
- `stopped`：同步停止状态并清理必要的高亮；
- `error`：使用现有错误/回退语义，不让两个窗口出现矛盾状态。

禁止让悬浮窗自行轮询或消费 TTS 队列。

### 6.3 关闭语义

- 关闭悬浮窗：只销毁悬浮显示，不停止朗读。
- 最小化主窗口：悬浮窗保持显示、置顶和可操作，朗读继续。
- 退出应用：保存进度和悬浮窗设置，停止朗读，取消回调，销毁全部窗口。
- 重复点击悬浮入口：聚焦或切换现有实例，不创建第二个实例。

## 7. 视觉复现要求

不要凭感觉写“macOS 风格”。编码时从原型提取并集中定义颜色、字号、间距、圆角、阴影、组件高度和状态色，再映射到桌面控件。

至少逐项对照：

- 主窗口画布、顶部栏、侧栏、主内容区的宽度比例；
- 三色窗口装饰、Logo、导航图标、头像、搜索框的位置；
- 内容库标题、说明、四个导入卡片和最近阅读卡片；
- 卡片封面比例、标题层级、进度条、格式标签和悬停反馈；
- 阅读器的目录宽度、正文最大阅读宽度、章节标题、正文行高；
- 底部播放条的固定位置、按钮间距、进度控件和悬浮入口；
- 粘贴文本弹窗的遮罩、尺寸、输入区、按钮和校验状态；
- 悬浮窗默认尺寸、圆角、透明度、文本层级、控制条和右下角缩放手柄；
- 主窗口变窄、悬浮窗最小尺寸、深色模式、无内容、不可用入口等状态。

Tkinter 对圆角、阴影、透明背景或图标渲染存在限制时，不得静默降级。先选择能最接近原型且可打包的实现；若确实无法等价实现，记录差异、原因和候选方案，交给用户确认。

## 8. 悬浮窗实现要点

- 必须是独立顶层窗口，不能是主窗口 Canvas 内的假浮层。
- 不要把悬浮窗设置成会随主窗口最小化的从属窗口；需要在 Windows 10/11 上实测最小化行为。
- 默认逻辑尺寸为 `560×270`，最小尺寸为 `430×238`。
- 右下角必须有与原型一致的可见缩放手柄，按下后可同时改变宽和高。
- 拖动仅由标题栏空白区域触发；按钮、滑块、文本选择和缩放手柄不得误触拖动。
- 拖动/缩放时连续更新窗口，释放鼠标后再持久化最终几何信息。
- 位置必须限制在可见屏幕范围；显示器断开或 DPI 改变后能回到可见区域。
- 默认置顶；置顶状态、透明度、字号和背景设置需要即时生效并保存。
- 正文区域随窗口宽度重排，底部控制区固定，不被长句挤出窗口。
- 窗口销毁后所有 `after` 回调、事件绑定和窗口引用必须安全清理。

## 9. 推荐实施顺序

### 阶段 0：锁定基线

- 检查当前工作区真实差异，不覆盖已有修改。
- 运行现有测试并记录基线结果。
- 画出书籍打开、章节切换、TTS 事件、进度保存和退出清理调用链。

### 阶段 1：统一视图状态

- 增加原型需要的设置默认值，保持旧 `library.json` 自动兼容。
- 抽出最小的 UI 状态同步入口，使主阅读区、底部播放条和悬浮窗共享状态。
- 此阶段不改变朗读引擎。

### 阶段 2：先完成悬浮朗读窗

- 完成独立窗口、文本层级、共享控制、置顶、关闭、拖动、缩放、最小化独立性和设置恢复。
- 用现有 TTS 事件验证当前句同步，不使用模拟朗读替代真实链路。
- 完成默认、最小尺寸和长句三种视觉对照。

### 阶段 3：严格实现主阅读器

- 按原型实现应用外壳、顶部工具栏、章节目录、正文和底部播放条。
- 接回现有章节、进度、搜索、书签、主题和 TTS 功能。
- 确认旧界面未被残留布局覆盖或挤压新界面。

### 阶段 4：实现内容库与导入

- 实现内容库卡片、搜索、空状态、文件导入和粘贴文本弹窗。
- 网页与音频入口按 P0 显示清晰的“后续支持/暂未支持”，不要做伪功能。

### 阶段 5：视觉与回归验收

- 在一致窗口尺寸和一致状态下，对原型与桌面实现逐屏截图对照。
- 修复明显的尺寸、间距、字体、颜色、圆角、阴影、对齐、裁切和状态差异。
- 完成数据兼容、TTS、长文本、DPI、多显示器和打包回归。

未完成当前阶段的验收，不进入下一阶段的扩展功能。

## 10. 测试与验证

### 10.1 自动化

根据现有项目命令运行完整测试，并至少新增以下覆盖：

- 新设置默认值、非法值回退和旧数据兼容；
- 悬浮窗几何解析、最小/最大边界和屏幕可见性；
- 同一时间只存在一个悬浮窗；
- 关闭悬浮窗不调用停止朗读；
- 主窗口和悬浮窗共享同一朗读状态；
- 上一句/下一句后的章节、偏移和高亮同步；
- 窗口销毁后回调不再访问控件。

### 10.2 手工验收

至少完成：

1. 启动进入内容库，检查空状态和有书状态。
2. 搜索并打开最近阅读内容，位置正确恢复。
3. 导入现有支持格式，粘贴有效文本与空文本。
4. 打开阅读器，切章节、调字号、开关目录、使用书签和搜索。
5. 播放、暂停、停止、前后句，核对主阅读区、播放条和悬浮窗同步。
6. 主窗口最小化，确认悬浮窗仍可见和可控制。
7. 拖动悬浮窗，向外放大、向内缩小，核对最小尺寸和文本重排。
8. 切换置顶、透明度、背景和字号，重启后核对恢复。
9. 在 100%、125%、150%、200% DPI 和双显示器环境中验证位置与缩放。
10. 使用 2 万字、10 万字内容持续朗读并操作窗口，检查卡顿与错误。
11. 源码运行和打包后的 exe 各验证一次。

### 10.3 视觉验收状态

至少保存并对照以下截图：

- 内容库默认状态；
- 内容库搜索结果和空状态；
- 粘贴文本弹窗；
- 阅读器目录展开状态；
- 阅读器目录收起状态；
- 底部播放条播放中和暂停状态；
- 悬浮窗默认尺寸；
- 悬浮窗最小尺寸；
- 悬浮窗放大状态；
- 深色模式。

视觉验收不是“看起来像”即可。每个截图需要记录可见差异，修复后再次对照；无法消除的差异必须明确列入交付说明。

## 11. 高风险点

- Tk 顶层窗口如果设置了不恰当的 owner/transient，主窗口最小化时可能一起消失。
- 自定义无边框标题栏可能破坏系统拖动、缩放、DPI 和多显示器行为。
- Windows 的顶层窗口圆角、阴影、透明度与点击区域可能存在平台限制。
- 高频 TTS 事件若触发全文扫描、组件重建或磁盘保存，会破坏现有长文本优化。
- 主阅读区和悬浮窗分别消费事件会造成重复播放、跳句或状态竞争。
- 拖动/缩放过程中写 `library.json` 会造成卡顿和文件竞争。
- `after` 回调未取消可能在关闭悬浮窗或退出应用后访问已销毁控件。
- PyInstaller 可能遗漏封面、图标、字体或新模块，必须做真实打包检查。

## 12. 完成交付物

- 严格按原型实现的桌面 UI。
- 接入现有朗读能力的独立悬浮朗读窗。
- 新增或更新的自动化测试。
- 原型与桌面实现的关键状态对照截图。
- README、快捷键说明、版本历史和打包资源更新。
- 一份交付说明，列出：实际修改、测试结果、手工验证环境、性能结果、未验证项和已知差异。

只有 `pd.md` 的 P0、视觉对照和本文件中的状态/兼容要求全部满足，才可以标记完成。

## 13. 可直接交给 Codex 的启动提示词

```text
请在 D:\phpstudy_pro\WWW\DDNovelReader 中实现多多朗读新版桌面 UI。

先完整阅读根目录 pd.md、HANDOFF.md、prototype/src/App.jsx、prototype/src/styles.css、prototype/design-qa.md、README.md，再阅读现有 novelreader 中与 GUI、导入、阅读、TTS、设置和快捷键有关的代码与测试。

这是严格的原型落地任务：页面结构、布局、尺寸、间距、配色、圆角、阴影、文案、图标位置和交互状态必须以 prototype 为准，不得沿用旧 Tkinter 界面自行改版。只复用现有朗读功能及其 SpeechController、事件、进度、字符偏移、逐句高亮、语速、音量和长文本性能链路；不得创建第二套 TTS 或播放状态。书籍解析、书架、章节、缓存、进度、书签、搜索和设置可作为数据/业务能力复用，但不能改变原型视觉。

只实现 pd.md 中的 P0。网页导入和音频/播客入口在未实现时显示明确说明，不得做伪功能；自动翻译不在范围内。

工作区可能包含用户已有修改。开始前先检查真实差异，禁止重置、覆盖或删除无关文件。先向我输出：需求理解、现有调用链、拟修改文件、状态共享方案、分阶段实现计划和验证计划；确认没有遗漏后再开始编码。每完成一个阶段都运行相关测试并做原型截图对照。最终给出修改清单、测试结果、手工验证结果、性能结果、未验证项和已知差异。
```
