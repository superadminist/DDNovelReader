# 多多朗读 macOS 风格桌面版产品需求文档

> 文档类型：Product Definition / PRD
>
> 状态：可进入开发
>
> 目标项目：`D:\phpstudy_pro\WWW\DDNovelReader`
>
> 交互原型：`D:\phpstudy_pro\WWW\DDNovelReader\prototype`
>
> 视觉参考：`E:\ForCodex\dailyRepair\video-ui-extraction\WeChat\_20260903141223\key-ui`

## 0. 交付原则与规范优先级

本次是“原型落地”，不是在现有界面上做风格微调。开发结果必须严格复现 `prototype` 中已经确认的视觉和交互；现有项目只提供可复用的数据、文件解析、存储和朗读能力，旧界面不构成新界面的视觉约束。

发生冲突时按以下优先级执行：

1. 本文档中的明确业务规则、验收标准和兼容要求。
2. `prototype/src/App.jsx` 与 `prototype/src/styles.css` 所呈现的页面结构、尺寸、间距、配色、圆角、阴影、文案、图标位置和交互状态。
3. `prototype/design-qa.md` 中已经确认的设计决策。
4. `key-ui` 原始截图，仅用于补充原型未覆盖的细节。
5. 现有桌面程序界面，仅用于确认已有功能入口和业务行为，不用于决定新界面外观。

严格复现包括但不限于：内容库、侧栏、阅读页、目录、导入弹窗、底部播放条、悬浮朗读窗、深浅色状态、悬停/选中/禁用状态、窗口缩放响应及原型中的文案。不得以“Tkinter 默认样式”“与现有界面一致”或“功能已存在”为理由替换、删减或重新设计原型。

允许复用现有实现的范围：

- 现有 `SpeechController`、SAPI5/Edge TTS、播放队列、朗读事件、字符偏移、逐句高亮、语速和音量逻辑必须直接复用。
- 书籍解析、章节数据、书架存储、阅读进度、书签、搜索、缓存和设置等非视觉业务能力应优先复用，避免数据迁移和行为回归。
- 不复用旧控件布局、旧菜单结构、旧视觉样式或旧窗口层级；新 UI 以原型为唯一视觉实现目标。

## 1. 背景

多多朗读当前是 Windows 本地桌面阅读器，已经具备多格式导入、书架、章节目录、阅读进度、全文搜索、书签、SAPI5/Edge TTS、逐句高亮、缓存、全屏和快捷键等能力。

本次需求不是重写朗读引擎，而是把已经确认的 macOS 风格原型完整落地为桌面应用，并新增真正独立的悬浮朗读窗口。现有功能、数据格式和朗读行为需要兼容，但旧界面无需保留。

核心问题：

1. 当前主界面功能完整，但信息密度高，缺少内容库首页和清晰的阅读层级。
2. 用户在使用其他软件时，无法只保留当前朗读内容和控制按钮。
3. 悬浮窗必须支持拖动与自由缩放，主窗口最小化后仍能显示和控制朗读。
4. 长文本性能优化已经存在，新增 UI 不得破坏该优化或恢复高频全量刷新。

## 2. 产品目标

### 2.1 必须达到

- 将桌面主界面实现为原型所示的 macOS 风格：窗口标题栏、侧栏、内容库、阅读器、目录和底部播放条。
- 严格复现原型的页面结构、视觉参数、文案、控件位置和核心交互，不允许开发阶段自行改版。
- 提供独立悬浮朗读窗口，展示上一句、当前句和下一句。
- 悬浮窗与主窗口共享同一个 TTS 状态和阅读位置。
- 悬浮窗支持拖动、放大、缩小、置顶、关闭、播放/暂停、停止、上一句和下一句。
- 主窗口最小化后，悬浮窗继续显示并继续朗读。
- 记住悬浮窗位置、尺寸、字号、透明度、背景和置顶设置。
- 保留现有书籍、缓存、进度、书签、搜索、主题、快捷键和 TTS 行为。
- 朗读功能直接接入现有实现；新 UI 只负责展示和发出控制意图，不另建朗读服务。

### 2.2 成功标准

- 用户能从内容库进入阅读，并在 2 次点击内打开悬浮朗读窗。
- 悬浮窗拖动和缩放反馈连续，没有明显跳动、闪烁或失去鼠标捕获。
- 主窗口与悬浮窗的播放状态、当前句和章节信息始终一致。
- 2 万字以上文本朗读、高亮和进度保存不出现因新界面引入的明显卡顿。
- 老用户原有 `library.json` 无需迁移操作即可继续使用。

## 3. 范围与优先级

### P0：本期必须完成

- macOS 风格应用框架和侧栏。
- 内容库首页和最近阅读卡片。
- 文件导入入口，复用现有多格式导入。
- 粘贴文本导入。
- 阅读器、章节目录、顶部工具栏和底部播放条。
- 独立悬浮朗读窗口及完整交互。
- 设置持久化、DPI、多显示器和最小化行为。
- 自动化测试与 Windows 手工验收。

### P1：本期建议完成

- 网页链接导入并提取正文。
- 双语内容显示：仅在已有译文或可确认的双语段落映射存在时开启。
- 内容库网格/列表切换。

### P2：后续迭代

- 播客/音频导入与语音转写。
- 鼠标穿透模式。
- 多套悬浮窗预设样式。
- 自动翻译服务。

### 明确不在本期范围

- 重写 `SpeechController` 或替换 SAPI5、Edge TTS、MCI 播放链路。
- 引入账号、云同步、远程数据库或在线书城。
- 为了 UI 重构书籍解析、章节切分、缓存格式或书签数据结构。
- 未确认服务和隐私方案前接入自动翻译、网页转写或音频识别 API。

## 4. 用户与核心场景

### 4.1 目标用户

- 长时间阅读小说、文章、电子书的 Windows 用户。
- 希望边工作边听文本、只保留字幕式朗读窗口的用户。
- 经常导入 TXT、EPUB、DOCX、PDF 等本地内容的用户。

### 4.2 核心路径

1. 启动应用。
2. 在内容库选择最近阅读内容，或导入文件/粘贴文本。
3. 进入阅读器，选择章节或从已有进度继续。
4. 点击“悬浮朗读”。
5. 最小化主窗口，继续通过悬浮窗查看当前句并控制朗读。
6. 拖动或缩放悬浮窗，应用退出后保存窗口设置。

## 5. 信息架构

主导航固定在左侧，包含：

- 内容库：默认内容管理入口。
- 阅读器：当前打开内容。
- 音频内容：P2 功能入口；未实现时必须明确显示“暂未支持”，不能假装导入成功。
- 深色模式：切换界面模式。
- 设置：进入现有设置、缓存管理、关于等功能。

启动兼容规则：

- 新用户、没有最近书籍或关闭“自动打开上次书籍”时进入内容库。
- 老用户若 `auto_open_last=true` 且最近书籍有效，继续保留自动进入续读；可通过侧栏返回内容库。
- 不擅自改变现有默认设置含义。

## 6. 功能需求

## 6.1 macOS 风格应用框架

### FR-001 窗口外观

- 主窗口保持 Windows 桌面应用，不伪装成真正的 macOS 系统窗口。
- 视觉采用浅灰蓝背景、半透明浅色面板、柔和阴影、圆角卡片和低饱和蓝紫强调色。
- 标题栏展示三色窗口装饰、应用名称、搜索入口和用户占位头像。
- 三色按钮若未绑定真实窗口行为，应作为装饰，不得误导为可用按钮；如绑定则必须分别对应关闭、最小化和最大化。
- 原型使用的 UI 图标应替换为项目可分发的真实图标资源或可合法使用的图标库。

### FR-002 窗口状态

- 主窗口支持最小化、最大化、恢复和关闭。
- 继续尊重 Windows DPI 感知。
- 主窗口关闭时必须停止 TTS、保存当前进度并销毁悬浮窗。

## 6.2 内容库

### FR-010 内容库首页

- 显示“内容库”标题、说明、搜索框、四个导入入口和最近阅读区域。
- 最近阅读按 `last_read_at` 倒序排列。
- 书籍卡片展示封面、标题、当前章节/最近位置、阅读百分比和格式。
- 没有封面时使用统一默认封面，不从截图中裁剪伪造封面。
- 单击卡片进入阅读器；双击行为可与单击一致，不新增冲突操作。

### FR-011 搜索

- 输入后按书名进行即时本地筛选。
- 搜索不触发磁盘重复解析，不修改书架数据。
- 无结果时显示明确空状态和“清空搜索”。

### FR-012 粘贴文本

- 点击后打开模态窗口，包含标题和正文。
- 标题可选；为空时用正文首行截断生成标题。
- 正文去除首尾空白后为空时禁止提交并提示原因。
- 成功后写入本地内容缓存、加入书架并进入阅读器。
- 必须定义稳定的本地来源标识，避免同标题文本相互覆盖。
- 粘贴内容的分章与朗读继续复用现有文本处理链路。

### FR-013 文件导入

- 复用现有 `_add_book`、拖拽、多选、解析、源文件备份和错误提示。
- 支持范围与 README 保持一致，不擅自删减格式。
- 导入成功后刷新内容库并打开最后成功导入的书籍。

### FR-014 网页链接

- P1 功能。
- 未实现正文提取前，入口显示“后续支持”或禁用说明。
- 如果实现，必须校验 HTTP/HTTPS、设置超时、限制响应大小，并在后台线程提取正文。
- 网页抓取失败不得创建空书籍或破坏现有书架。

### FR-015 播客/音频

- P2 功能。
- 当前项目没有语音转写链路，本期只能展示入口和“暂未支持”状态。
- 禁止用现有 TTS 播放能力冒充音频转写或播客导入能力。

## 6.3 阅读器

### FR-020 顶部工具栏

- 包含返回内容库、目录开关、书名、字号减小、字号增大、排版、悬浮朗读和设置。
- 字号操作复用现有 `_change_font_size`，并保持主阅读区与设置状态一致。
- 目录开关复用现有 `_toggle_toc` 语义。
- 悬浮朗读按钮反映打开/关闭状态。

### FR-021 章节目录

- 显示章节序号、标题、总章节数和当前章节高亮。
- 点击章节复用 `_goto_chapter`。
- 章节切换时更新正文、进度、播放条和悬浮窗。
- 不改变现有章节解析结果。

### FR-022 正文区域

- 显示章节编号、章节标题、正文和阅读页码/总进度信息。
- 朗读当前句保持高亮，并复用现有 `_highlight_sentence` 与位置映射。
- 不为悬浮窗复制整章文本到新的长期缓存；仅传递需要显示的相邻句信息。
- 目录、字号、窗口变化后不得恢复全章扫描或每句重建主界面。

### FR-023 底部播放条

- 显示当前章节、朗读状态、上一句、播放/暂停、下一句、停止、进度、语速、音量和悬浮窗入口。
- 播放/暂停/停止必须复用现有 `_tts_toggle`、`_tts_stop` 和 `SpeechController` 实例。
- 播放状态改变时，主工具栏、底部播放条和悬浮窗同时更新。
- 语速和音量继续写入现有设置。

## 6.4 悬浮朗读窗口

### FR-030 生命周期

- 悬浮窗是独立桌面窗口，不是主阅读区内的浮层。
- 同一时间最多存在一个悬浮窗实例。
- 没有打开书籍时点击入口，提示先选择内容，不创建空窗口。
- 关闭悬浮窗只关闭显示，不停止朗读。
- 退出主程序时关闭悬浮窗并完成现有退出保存流程。
- 主窗口最小化后悬浮窗仍保持可见、可操作和持续朗读。

### FR-031 显示内容

- 标题栏：当前章节名、朗读状态点、置顶按钮、关闭按钮。
- 内容区：上一句/上下文、当前句、可选译文、下一句。
- 当前句使用最大字号和最高对比度；上下文与下一句弱化。
- 文本过长时内容区内部滚动，控制区固定在底部。
- 发生章节切换时立即更新章节名和相邻句。

### FR-032 播放控制

- 支持上一句、播放/暂停、下一句和停止。
- 控制的是主应用唯一的 `SpeechController`，不得启动第二个 TTS 线程或第二路音频。
- 点击上一句/下一句后，主阅读区高亮、字符偏移、状态栏和悬浮窗同步。
- 播放结束、停止或错误时，悬浮窗状态同步更新。

### FR-033 拖动

- 标题栏空白区域用于拖动。
- 点击置顶、关闭等按钮时不得触发拖动。
- 拖动必须在按下、移动、释放完整生命周期内保持鼠标捕获。
- 窗口至少保留 24 px 可见区域，不能完全移出所有显示器。
- 多显示器变化后，如果保存位置不再可见，恢复到主显示器可见区域。

### FR-034 缩放

- 右下角提供明确可见的缩放手柄，鼠标显示斜向缩放指针。
- 按住手柄可同时改变宽度和高度；向外放大，向内缩小。
- 默认建议尺寸：`560×270` 逻辑像素。
- 最小建议尺寸：`430×238` 逻辑像素；低于该尺寸会影响控件完整性，必须阻止。
- 最大尺寸不超过当前显示器工作区，并保留至少 10 px 安全边距。
- 缩放时正文重新排版，底部控制条不得被遮挡或漂移。
- 松开鼠标后保存最终尺寸，拖动过程中不频繁写磁盘。

### FR-035 置顶、透明度与背景

- 默认置顶开启，按钮状态清晰可见。
- 支持关闭置顶。
- 透明度建议范围 `0.65–1.00`，默认 `0.92`。
- 背景至少支持浅色、米黄、深色三种预设。
- 透明度只影响悬浮窗视觉，不应导致文字对比度不可读。

### FR-036 字体

- 支持悬浮窗字号减小/增大。
- 建议范围 `14–40`，默认 `22`。
- 字体默认沿用主阅读器字体；用户可选择“跟随主界面”或独立字号。
- 调整后即时生效，松开/点击完成后持久化。

### FR-037 双语显示

- 原型中的“中 / EN”代表显示已有译文，不代表自动翻译。
- 只有当前句存在可靠译文映射时显示切换按钮。
- 没有译文时隐藏或禁用按钮并提示“当前内容无译文”。
- 自动翻译属于 P2，未确认服务前禁止调用外部接口。

## 6.5 状态同步

### FR-040 单一事实来源

以下状态继续由现有应用对象持有：

- `current_bid`
- `book`
- `chapter_idx`
- `char_offset`
- `SpeechController` 播放/暂停/停止状态
- `settings`

悬浮窗只订阅这些状态的变化，不保存独立阅读进度。

### FR-041 TTS 事件

必须覆盖现有事件：

- `sentence_start`：更新主阅读高亮、悬浮窗当前句和章节。
- `sentence_done`：更新字符偏移、进度和保存节流。
- `finished`：更新完成状态并保存。
- `stopped`：停止状态同步并清理高亮。
- `error`：显示非阻塞错误；Edge 回退本地语音时不能错误地重置播放状态。

建议在现有 `_handle_tts_event` 完成原行为后调用统一的 UI 同步方法，避免各窗口分别消费事件队列。

## 7. 设置与数据兼容

继续使用 `%APPDATA%\DDNovelReader\library.json` 的 `settings`，利用现有默认值合并机制兼容旧数据。

建议新增键：

| 键 | 默认值 | 说明 |
| --- | --- | --- |
| `floating_reader_geometry` | `""` | 悬浮窗宽高和位置 |
| `floating_reader_topmost` | `true` | 是否置顶 |
| `floating_reader_opacity` | `0.92` | 透明度 |
| `floating_reader_font_size` | `22` | 悬浮窗字号 |
| `floating_reader_follow_font` | `true` | 是否跟随主阅读器字体 |
| `floating_reader_background` | `"light"` | 背景预设 |
| `floating_reader_bilingual` | `false` | 双语显示偏好 |
| `library_view_mode` | `"grid"` | 内容库视图模式 |

约束：

- 不持久化临时播放状态。
- 不修改现有书籍、进度、书签和缓存字段含义。
- 保存几何信息应在拖动/缩放结束后执行，不能在每个鼠标移动事件中写 `library.json`。
- 几何值非法、屏幕已移除或 DPI 改变时使用安全默认值。

## 8. 错误与边界状态

- 无书籍：内容库展示空状态和导入入口。
- 无章节/空内容：复用现有空内容处理，不创建可朗读的空句。
- TTS 不可用：展示现有错误信息，悬浮窗保持可关闭。
- Edge 断网：继续复用本地语音回退逻辑。
- 主窗口最小化：悬浮窗继续工作。
- 主窗口恢复：不得创建第二个悬浮窗。
- 显示器断开：窗口回到可见区域。
- DPI 变化：字号、圆角、拖动边界和最小尺寸使用逻辑像素计算。
- 透明度配置非法：限制到允许范围并回退默认值。
- 悬浮窗正在拖动/缩放时退出：安全释放鼠标捕获，再执行退出流程。

## 9. 非功能需求

### 9.1 性能

- 不新增每 100 ms 全量重建书架、全文扫描或磁盘保存。
- 悬浮窗更新只更新发生变化的标签和按钮。
- TTS 事件继续由主线程定时消费，所有 Tk 控件更新必须在主线程执行。
- 2 万字内容持续朗读时，主窗口和悬浮窗交互应保持响应。
- 至少使用 10 万字样本执行持续朗读和窗口操作回归；100 万字作为压力测试而非每次提交的必跑项。

### 9.2 可靠性

- 悬浮窗异常关闭不影响主阅读器。
- 设置保存失败不能中断朗读。
- 所有 `after` 回调在窗口销毁后不得继续访问已销毁控件。

### 9.3 可访问性

- 所有图标按钮提供文本提示或可访问名称。
- 键盘焦点清晰可见。
- 不只依赖颜色表达播放、置顶和选中状态。
- 深色/浅色背景下当前句对比度保持可读。

### 9.4 兼容性

- Windows 10、Windows 11。
- 常见系统缩放：100%、125%、150%、200%。
- 单显示器、双显示器、主副屏左右排列。
- 源码运行和 PyInstaller onefile 包均可使用。

## 10. 快捷键

保留现有快捷键，不占用已存在组合。

建议新增：

- `Ctrl+Shift+F`：打开/关闭悬浮朗读窗。
- 悬浮窗聚焦时 `Space`：播放/暂停。
- 悬浮窗聚焦时 `Esc`：仅关闭悬浮窗。

新增前必须搜索冲突，并在快捷键说明和版本历史中同步记录。

## 11. 验收标准

### AC-01 内容库

- 启动满足内容库条件时显示内容库首页。
- 搜索能即时筛选书籍。
- 选择书籍能进入正确阅读位置。
- 文件导入继续支持现有格式。
- 粘贴有效文本后创建本地内容并进入阅读器；空文本无法提交。

### AC-02 阅读器

- 目录切换、章节切换、字号调整、搜索、书签和现有主题功能正常。
- 当前句高亮、主进度和底部播放条同步。
- 打开/关闭目录不会遮挡顶部工具栏或底部播放条。

### AC-03 悬浮窗

- 点击入口只创建一个独立窗口。
- 标题栏可拖动；按钮点击不误触拖动。
- 右下角手柄可放大和缩小宽高，并遵守最小/最大边界。
- 主窗口最小化后仍可见、可播放/暂停/停止、可前后跳句。
- 当前句、章节、播放状态与主窗口一致。
- 关闭悬浮窗不停止朗读。
- 退出应用后重新打开，位置、尺寸、字号、透明度、背景和置顶偏好恢复。

### AC-04 兼容与性能

- 旧 `library.json` 可直接加载。
- 现有测试无新增失败。
- 2 万字、10 万字样本朗读时没有因悬浮窗造成的持续卡顿。
- 多显示器/DPI 环境下悬浮窗不会永久落在不可见区域。
- 打包后的 exe 中图标、主题和悬浮窗功能完整。

## 12. 测试矩阵

| 类别 | 必测内容 |
| --- | --- |
| 单元测试 | 设置默认值与非法值回退、几何解析、边界限制、相邻句计算 |
| GUI 冒烟 | 创建/关闭悬浮窗、重复打开、主窗口最小化、退出清理 |
| 状态同步 | 播放、暂停、停止、上一句、下一句、章节切换、播放结束、错误回退 |
| 持久化 | 拖动/缩放后重启恢复；旧设置文件兼容 |
| 视觉 | 内容库、阅读器、粘贴弹窗、默认悬浮窗、最小尺寸、最大尺寸、深色背景 |
| 性能 | 2 万字必测、10 万字回归、100 万字压力测试 |
| 显示环境 | 100/125/150/200% DPI，单屏/双屏，副屏断开后恢复 |
| 打包 | 源码运行与 PyInstaller onefile 各验证一次 |

## 13. 完成定义

只有同时满足以下条件才能标记完成：

- P0 功能全部实现并通过验收。
- 原型关键画面已完成实现对照。
- 悬浮窗真实支持拖动和双向缩放，不只是视觉展示。
- 主窗口与悬浮窗共享状态，无第二套 TTS。
- 旧数据、现有功能和性能优化未被破坏。
- 测试结果、未验证项和已知限制写入交付说明。
- README、快捷键说明、版本历史和打包配置按实际改动同步。
