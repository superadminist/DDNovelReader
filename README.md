# 多多朗读（DDNovelReader）

一款 Windows 本地运行的中文小说阅读器，支持 **TTS 语音朗读**、主流电子书格式、书架管理、
阅读进度自动保存与自动续读、整本语音缓存。

## 功能一览

| 功能 | 说明 |
| --- | --- |
| 主流格式 | txt / epub / mobi / azw3 / pdf / docx / html / zip（自动解包查找内部小说） |
| TTS 语音朗读 | 默认本地语音（SAPI5，离线），可选 Edge 神经语音（晓晓/云希等 9 种中文音色，需联网）；开始/结束两个按钮；语速可调；句间隔可调；逐句高亮跟随（高亮固定窗口首行）；Edge 断网自动回退本地语音 |
| 音量控制 | 功能区音量滑条，通过 Windows Core Audio 控制本进程音量，不影响系统音量和其他软件 |
| 整本语音缓存 | 提前缓存整本书语音，网络不稳定时减少卡顿；支持按章节选择、暂停/继续、智能播放键（自动检测任务状态）、停止、进度持久化（重启后续传且自动补齐缺失章节）、缓存完成后自动关机；合成失败如实记录进度，不再谎报 100%；新增『验证补全』：重建任务列表逐句比对磁盘，精确报告缺失句数与章节，一键补齐 |
| 新版桌面界面 | 按确认原型实现 macOS 风格标题栏、固定侧栏、内容库首页、最近阅读卡片、阅读器、目录与底部播放条；支持浅色/深色状态 |
| 内容导入 | 文件导入继续支持现有全部格式；新增粘贴文本并立即阅读。网页链接为后续支持，播客/音频暂未支持，不会创建伪内容 |
| 悬浮朗读 | 独立桌面窗口显示上一句/当前句/下一句，与主窗口共用唯一 TTS；支持拖动、双向缩放、置顶、透明度、浅色/米黄/深色背景、字号、播放暂停、停止和前后跳句 |
| 阅读书页配色 | 白天 / 护眼 / 夜间 / 米黄 四种阅读区配色 |
| 字体设置 | 任意系统字体、字号（10–48）、行距（1.0–3.0）可调，实时生效 |
| 总进度 | 底部进度滑块 + 百分比 + 「第 X 章 / 共 Y 章」+ 总字数，实时更新；拖动滑块跳转；百分比可点击手动输入 |
| 自动保存 | 滚动、翻章、朗读、退出时自动保存阅读进度 |
| 自动续读 | 每次打开自动回到上次阅读位置，并自动打开最近阅读的书 |
| 书架 | Windows 详细信息视图（进度/书名/大小/时间，可排序带箭头）；支持多选；右键菜单（打开/复制原文件/复制书名/删除/删除文件）；书架与阅读区可拖拽调节宽度；默认关闭；导入时自动备份源文件，原文件删除后阅读/重新分章/复制原文件照常，备份缺失时复制原文件置灰 |
| 章节切分 | 三重检测：严格匹配（第X章）/ 分隔线匹配 / 书名前缀匹配（如"书名 第1章"）；另识别正文中穿插的『第X章 标题』内层章节（编号连续时优先按内层真章节分章，支持嵌在正文行中间、分隔符兼容空格/冒号）；支持楔子/序章/番外等；第一章前文字归入"简介"章 |
| 章节标题 | 阅读区每章开头显示当前章节标题（加大、粗体、居中） |
| 拖拽导入 | 把文件直接拖进书架区即可导入 |
| 书签 + 划线 + 笔记 | 选中文字右键「添加书签/划线」弹出备注框（默认取选中文字），目录区顶部「目录/书签」切换；书签浅绿高亮、一键跳转、右键删除 |
| 全文搜索 | 功能区 🔍 按钮，书内关键词搜索，双击结果跳转定位 |
| 书架备份/还原 | 书架底部按钮，一键导出/导入书架、进度、设置与书签（还原前自动备份当前数据） |
| 阅读区右键 | 复制 / 百度搜索 / 谷歌搜索 / 必应搜索 / 翻译 / 从该段开始朗读 |
| 全屏模式 | F11 或 Alt+Enter 进入；右上角显示当前时间、阅读时间、章节名、总进度；自动隐藏书架与目录（退出按原状态恢复） |
| 定时停止 | 可设置分钟数，到时自动停止朗读 |
| 滚动条统一 | 书架/目录/阅读区/下载管理器/全文搜索/更新记录/快捷键/缓存管理 全部统一扁平黑色滑块样式并加宽（35px），夜间主题自动切换深色 |
| 空行压缩 | 三种模式：不压缩 / 合并为一行 / 清理所有行；段落首行缩进二字 |
| 缓存管理 | 正文缓存 + 音频缓存均可自定义文件夹、一键转移、一键清除、打开文件夹、实时统计大小 |
| 启动体验 | 秒开：窗口约 1 秒显示并带阅读区加载动画，书籍后台加载，不再白屏等待 |
| 窗口 | 默认 1280×720 并居中显示，每次启动固定 |

## 下载

在 [Releases](https://github.com/caimttth3-eng/DDNovelReader/releases) 页面下载最新版 `DDNovelReader-vX.XX.exe`，双击即可运行，绿色免安装。

## 使用步骤

1. 打开程序，在「内容库」导入文件或粘贴文本
2. 单击最近阅读卡片进入阅读器；目录可切章，底部播放条控制朗读
3. 点击顶部或底部的「悬浮朗读」，可在主窗口最小化后继续查看与控制
4. 设置中可调整字体、段落、书页、语音、悬浮窗透明度/字体/背景，并进入缓存、搜索、书签和关于
5. F11 进入全屏模式；Ctrl+Shift+F 打开或关闭悬浮朗读窗

## 数据存放位置

- 书架与阅读进度：`%APPDATA%\DDNovelReader\library.json`
- 书籍解析缓存：`%APPDATA%\DDNovelReader\cache\`
- 源文件备份：`%APPDATA%\DDNovelReader\cache\sources\`（导入书籍时自动备份原文件，原文件删除后仍可重解析 / 复制）
- 语音缓存：`%APPDATA%\DDNovelReader\tts_cache\`（可在"关于→缓存管理"中自定义位置）

## 常见问题

- **没有中文语音？** 在 Windows「设置 → 时间和语言 → 语音」中添加中文语音包后重启程序。
- **Edge 语音无声？** 确认网络正常；Edge 语音需联网，断网时自动回退本地语音。
- **朗读卡顿？** 可使用"整本语音缓存"提前缓存，或切换到本地语音。
- **分章不正确？** v1.95 重构分章系统支持"书名 第N章"格式，v1.99V2 进一步识别正文中穿插的『第X章 标题』内层章节，v1.99V3 支持章节标题嵌在正文行中间、分隔符兼容空格/冒号；如仍有问题可删除书籍重新导入。
- **音量滑条无效？** 确保程序正在播放音频（音频会话存在时音量控制才生效）。

## 开发说明

### 环境要求
- Python 3.10+
- Windows 10/11

### 安装依赖
```bash
install.bat
```
或手动：
```bash
python -m venv .venv
.venv\Scripts\pip install -r requirements.txt
```

### 运行
```bash
run.bat
```
或手动：
```bash
.venv\Scripts\python -m novelreader.main
```

### 打包 exe
```bash
build.bat
```
或手动：
```bash
.venv\Scripts\python -m PyInstaller --clean 多多朗读.spec
```
产物在 `dist\多多朗读.exe`。

### 运行测试
```bash
.venv\Scripts\python tests\test_core.py
.venv\Scripts\python tests\test_gui.py
.venv\Scripts\python tests\test_features.py
```

## 目录结构

```
DDNovelReader/
├── README.md                    # 项目说明
├── ROADMAP.md                   # 开发路线图
├── requirements.txt             # Python 依赖
├── .gitignore
├── install.bat                  # 安装依赖
├── run.bat                      # 运行程序
├── build.bat                    # 打包 exe
├── 多多朗读.spec                 # PyInstaller 打包配置
├── runtime_hook_tkinter.py     # PyInstaller 运行时钩子（tkinter 路径修复）
│
├── novelreader/                 # Windows 版源码
│   ├── __init__.py              # 版本号
│   ├── main.py                  # 入口
│   ├── gui.py                   # 主界面（主题系统/书架/阅读区/全屏）
│   ├── modern_ui.py             # 新版原型桌面框架、内容库、阅读器与悬浮朗读窗
│   ├── book_loader.py           # 各格式解析
│   ├── chapterizer.py           # 章节切分（三重检测）
│   ├── storage.py               # 书架/进度/设置/缓存
│   ├── tts_engine.py            # 语音朗读引擎（本地+Edge+整本缓存+音量控制）
│   ├── textproc.py              # 文本预处理（显示用/朗读用双轨）
│   ├── version_info.py          # 版本历史与快捷键说明
│   ├── reader_ui.py             # 阅读区渲染/高亮/进度（Mixin）
│   ├── shelf_ui.py              # 书架区（含备份/还原，Mixin）
│   ├── import_ui.py             # 书籍导入/拖拽/阅读区右键（Mixin）
│   ├── tts_ui.py                # 朗读控制条/状态栏（Mixin）
│   ├── theme_ui.py              # 主题系统（Mixin）
│   ├── dialog_ui.py             # 通用对话框/关于（Mixin）
│   ├── cache_ui.py              # 缓存管理（Mixin）
│   ├── download_ui.py           # 下载管理器/整本缓存（Mixin）
│   ├── shortcuts_ui.py          # 快捷键（Mixin）
│   ├── bookmark_ui.py           # 书签/划线/笔记（Mixin）
│   ├── search_ui.py             # 全文搜索（Mixin）
│   └── constants.py             # 常量与主题配色
│
├── dd_novel_reader/             # 安卓 Flutter 工程（开发中）
│
├── assets/                      # 资源文件（图标、皮肤预览等）
├── tests/                       # 自动化测试
├── sample/                      # 测试样书
├── docs/                        # 文档与预览（主题/图标/功能区预览）
├── releases/                    # 版本归档
│
├── dist/                        # 打包产物（gitignore）
├── build/                       # 构建缓存（gitignore）
└── .venv/                       # Python 虚拟环境（gitignore）
```

## 技术栈

- 界面：Python 标准库 tkinter（Tcl/Tk 8.6）+ ttk 主题
- 朗读：pyttsx3（Windows SAPI5）+ edge-tts（微软 Edge 神经语音）
- 音频播放：Windows MCI（winmm.dll，零第三方依赖）
- 音量控制：pycaw（Windows Core Audio，进程级音量）
- 解析：ebooklib（epub）、mobi（mobi/azw3）、pypdfium2（pdf，PDFium 引擎）、python-docx（docx）、zipfile（zip）
- 拖拽：tkinterdnd2（Tcl tkdnd 扩展）
- 打包：PyInstaller onefile（内置 Tcl/Tk 与全部依赖，绿色免安装）

## 作者

- 邮箱：230468896@qq.com

## 许可证

MIT License

## 版本历史

### v2.0.0（2026-09-12）
- 严格按确认原型落地新版桌面框架：标题栏、固定侧栏、内容库、阅读页、目录与底部播放条
- 新增粘贴文本导入；网页链接明确为后续支持，播客/音频明确为暂未支持
- 新增独立悬浮朗读窗，支持拖动、缩放、置顶、透明度、三种背景、字号及完整播放控制
- 主窗口与悬浮窗共用唯一 `SpeechController`、事件队列、字符偏移、高亮与进度保存链路
- 新增 Ctrl+Shift+F；悬浮窗 Space 播放/暂停、Esc 仅关闭悬浮窗

### v1.99V3（2026-09-09）
- 分章器再升级：600 个外层假标题 + 361 个内层真标题 → 正确识别为 362 章；章节标题可嵌在正文行中间（前置为上一章正文），分隔符兼容 空格/全角空格/冒号；缓存版本升至 8，旧书自动重解析
- 修复行首标题误判：『第X章』后无分隔符直接接正文（如正文行『第一章正文内容。』）不再被当成章节标题，不再吞正文/产生空章
- 新增『验证补全』：整本缓存管理器逐句比对磁盘，精确报告缺失句数与章节，一键补齐缺失音频；修复下载管理器进度显示（读活跃任务真实状态，不再误显示『已暂停』）
- 新增源文件备份：导入自动备份原文件到缓存目录 sources/，原文件删除后阅读/重新分章/复制原文件照常；备份缺失时复制原文件菜单置灰；删除书籍同步清理备份

### v1.99V2（2026-09-04）
- 分章器升级：识别正文中穿插的『第X章 章节名』内层章节标题（编号连续时优先按内层真章节分章）；自动清理残留外层大段标题；缓存版本升至 7，旧书自动重解析
- 下载管理器按钮优化：开始/继续/暂停合并为智能播放键（自动检测任务状态：▶开始/⏸暂停/▶继续/✔已完成），恢复独立「停止」按钮
- 修复整本缓存进度谎报：Edge 合成失败不再标记完成（此前显示 100% 实际缺整章，朗读到缺失章节仍联网）；恢复缓存自动校验实际文件并补齐缺失章节
- 修复打开整本缓存窗口白屏 / 未响应 / 硬盘占用拉满：完成态文件校验从主线程移到后台单线程（主线程毫秒级返回，不再扫描磁盘卡 UI）
- 全软件滚动条统一：书架/目录/阅读区/下载管理器/全文搜索/更新记录/快捷键/缓存管理 全部统一扁平黑色滑块样式并加宽（35px），夜间主题自动切换深色
- 修复窗口模式打开目录后进入全屏目录残留：全屏同时隐藏书架与目录，退出按原状态恢复

### v1.99V1（2026-09-04）
- 快捷键全面整理：导航（Ctrl+PageUp/PageDown 上下章、Ctrl+L 目录）、朗读（Ctrl+P 暂停/继续、Ctrl+S 停止、Ctrl+R 从屏顶段落朗读）、排版（Ctrl+Alt+Plus/Minus 行距、Ctrl+M 空行模式）、参数（Ctrl+↑/↓ 音量、Ctrl+←/→ 语速、直接按 +/- 字号、0 重置）、配色（Ctrl+1-4）、功能（Ctrl+D 下载管理器、Ctrl+T 定时）
- 代码结构重构：gui.py 3700+ 行单类拆分为 9 个 Mixin 模块 + constants.py，功能不写入 gui.py
- 新增书签 + 划线高亮 + 备注笔记（合并功能）：选中文字右键「添加书签/划线」弹备注框（默认取选中文字可直接确认），目录区顶部「目录/书签」切换，书签浅绿高亮、一键跳转、右键删除
- 新增全文搜索：功能区 🔍 按钮（书页选项后），书内关键词搜索，双击结果跳转
- 新增书架备份 / 还原：一键导出/导入书架、进度、设置与书签，还原前自动备份当前数据
- 修复下载管理器打开即报错导致记录消失（_cache_bar_str 缺 self）
- 下载管理器进度条升级：20 格 + 1/8 精度平滑块

### v1.99（2026-09-03）
- 启动秒开：窗口先显示（约 1 秒）并带阅读区加载动画，书籍后台加载，不再白屏等待
- 旧缓存自动重解析：缓存版本校验升级，打开书籍时自动重新解析，修复旧版缓存数据异常
- 默认窗口 1280×720 并居中显示，每次启动固定
- 音频缓存目录结构重构：按书分目录（tts_cache/书籍/语音/语速），旧数据启动自动一次性迁移
- 新增音频缓存大小索引：不再逐文件遍历磁盘（避免硬盘持续读取），关于/书架/下载管理器三处大小统一读索引
- 新增下载管理器：整本缓存升级为多书任务列表页，支持多书同时缓存、批量开始/暂停/继续/停止，双击行进入章节选择
- 修复整本语音缓存无法缓存的问题（此前表现为第二本书无法缓存）
- 缓存任务优先复用已解析的正文缓存，缓存更快更稳
- 修复切换书籍后缓存状态串扰：书架/状态栏/缓存窗口统一绑定当前书籍
- 修复重新缓存时任务列表不重建：改选章节或全量续传时自动重建，已缓存文件自动跳过
- 缓存任务构建大幅提速：百万字书任务构建从约 99 秒降至约 2 秒

### v1.98（2026-09-02）
- 新增 ZIP 格式支持：自动解包查找内部 txt/epub，损坏/加密/无小说文件明确报错
- 导入超过 25MB 的 ZIP 文件时弹出确认窗口
- 新增拖拽导入（tkinterdnd2，替代易闪退的 windnd 方案）
- PDF 解析引擎替换：PyMuPDF → pypdfium2，安装包 48MB → 32.1MB
- 新增章节标题显示：阅读区每章开头显示当前章节标题（加大、粗体、居中）
- 修复朗读高亮落到下一句（Tk Text 行号偏移）与高亮结束位置错误

### v1.97（2026-09-01）
- 新增音量滑条（pycaw Core Audio 进程音量控制）
- 新增全控件主题系统（6套 A-F），关于界面点击图标进入主题选择
- 新增米黄书页配色，新用户默认使用米黄暖读主题
- 新图标（OpenRunde 白色双 DD，#202020 圆角底）
- 书架布局重构：移至内层，默认关闭，开关不再挤占功能区
- 功能区拆分为两行，窗口模式下所有按钮可见
- 关于界面窗口增大，所有硬编码白色背景改为动态主题色

### v1.96（2026-09-01）
- 修复启动慢与硬盘持续被读取：书架「大小」列改为持久化索引
- 音频播放器改为 Windows 原生 MCI，移除 pygame 依赖
- 安装包瘦身：49.6MB → 35.5MB

### v1.95（2026-08-31）
- 重构分章系统，支持"书名 第N章"格式，修复多章节被分成同一章节的问题
- 第一章前文字归入"简介"章

### v1.94（2026-08-29）
- 可拉伸布局：书架区/目录区与阅读区之间可拖拽调节宽度
- 书架重构为 Windows 详细信息视图（进度/书名/大小/时间，可排序带箭头），支持多选
- 整本语音缓存重构为持久化进度，重启后「继续上次下载」从上次位置精确续传
- 修复大量书籍+大量音频缓存时启动卡死

### v1.93（2026-08-29）
- 所有弹出窗口初始位置改为屏幕正中间
- 新增音频缓存管理：自定义缓存文件夹、一键转移缓存、清除、打开文件夹、实时统计
- 书架显示文本缓存+音频缓存总大小
- 右下角状态栏：整本缓存状态可点击暂停/继续，阅读进度百分比可点击手动输入
- 软件关闭时自动暂停音频缓存

### v1.92（2026-08-29）
- 双文本底层：显示保留原文标点，TTS 朗读用纯净文本（消除连续标点/空白静音卡顿）
- 语音缓存详细管理窗口（开始/暂停/继续、章节选择、续传、容量统计、自动关机）
- 阅读区右键「从该段开始朗读」
- 功能区「定时」按钮（分钟为单位，到点自动停止朗读）
- 书架区右键「添加书籍」

### v1.91（2026-08-28）
- 新增整本语音缓存（独立后台线程，缓存期间不影响朗读）

### v1.9（2026-08-28）
- 修复「清理所有行」空行模式朗读高亮无法跟随
- 修复朗读中手动切换章节后朗读停止

### v1.8（2026-08-28）
- 顶部按钮区美化、兼容 Windows 百分比缩放（DPI 感知）
- 新增 DD 程序图标、修复空行三模式高亮跟随、修复断网回退后继续按钮

### v1.7（2026-08-28）
- 软件更名多多朗读（DDNovelReader）、多书批量导入带进度窗
- 数据迁移到 %APPDATA%\DDNovelReader、空行压缩、句子停顿、TTS 缓存进度

### v1.6（2026-08-28）
- 书架一键收起/展开、全屏模式优化、朗读预取缓存扩展到 30 句
- 分章不再吞字（第一章前文字归入简介章）、关于新增缓存管理

### v1.5（2026-08-28）
- 阅读区右键（复制/百度/谷歌/必应搜索/翻译）、书架右键菜单
- 关于对话框、快捷键、全屏模式

### v1.4（2026-08-28）
- TTS 按钮精简为两个、默认本地语音、阅读区高亮钉顶修复
- 可选 Edge 神经语音（9 种中文音色）
